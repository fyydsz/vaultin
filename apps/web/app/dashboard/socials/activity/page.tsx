"use client";

import { HeartHandshakeIcon } from "lucide-react";
import { PlaceholderSection } from "@/components/placeholder-section";

export default function SocialsActivityPage() {
  return (
    <PlaceholderSection
      icon={HeartHandshakeIcon}
      category="Community & Friends"
      title="Activity Feed"
      description="View real-time saving milestones and progress updates from your circle, send encouragement reactions, and stay motivated together."
    />
  );
}
